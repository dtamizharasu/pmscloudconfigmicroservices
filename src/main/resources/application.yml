server:
  port: 8089

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tamizh1767/config-server-properties
          clone-on-start: true